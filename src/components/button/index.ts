import Root, {
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
  buttonVariants,
} from './Button.svelte';

export {
  type ButtonProps as Props,
  Root as AppButton,
  buttonVariants,
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
};
