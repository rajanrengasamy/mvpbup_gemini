export interface DemoModelRequest {
	id: string;
	name: string;
	description: string;
	createdAt: string;
	updatedAt: string;
}

export interface DemoModelResponse {
	id: string;
	experienceId: string;
	createdAt: string;
	updatedAt: string;
}
